import { PreviewComponent } from '../preview/preview.component';

// Define the map of custom element selectors to their Angular components
export const selectorComponentMap = new Map<string, any>([
	['nde-record-actions-before', PreviewComponent],
]);
